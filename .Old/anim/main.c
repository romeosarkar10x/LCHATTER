#include <stdio.h>
// #include "include/load.h"
// #include "include/anim.h"
int main ()
{
    // load_ ("source/logo1.txt");
    // _animate ();
    for (int i = 0; i < 256; i++)
    {
        printf ("(%c) cdcd \n", i);
    }
    return 0;
}